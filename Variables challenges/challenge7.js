const prompt = require('prompt-sync')();
    let r = Number(prompt('Entree le rayon du cercle : '));
    let c = 2 * Math.PI * r ;
        console.log(`La circonférence de cercle est : ${c}`);
    console.log('kgdudflmh')